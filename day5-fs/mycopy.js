const fs = require('fs');
const colors = require('colors')
console.log('hello'.green);
/**
 *  @function [判断文件是否存在]
 *  @function [拷贝文件]
 */

 const copy = () => {

 }
 const exists = () => {

 }
 console.log(`${__dirname} + '/src'`.inverse)

 exists(__dirname + '/src', __dirname+'/dist', copy)