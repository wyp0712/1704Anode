const { add } = require('./main');
const  { noRepeat, Arrayfrom, myReduce, makeIterator}  = require('./util');
const dataJson = require('./data.json'); // json引入必须有后缀名
// 抛出对象必须用module.exports

console.log(noRepeat, 'noRepeat' )


// console.log(exports === module.exports)
// module.exports = {

// }
