/**
 * @param { promise }
 * 
 */

const arr = ['黑暗森林', '前任', 'hello world', 'vue', 'react'];
const brr = ['1', '8', '6', '5'];
const crr = [1,2,8,7,8,9,4,3,2];

// fiter

function sortFn(arr) {
  return arr.sort((a, b) => a < b)
}
console.log(sortFn(crr))





